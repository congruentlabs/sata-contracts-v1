// TODO(Phase 4): rewrite against current SignataRight + SignataIdentity ABI.
//
// The pre-modernization test depended on the legacy SignataIdentity create()
// signature (see SignataIdentity.test.js note). Skipped here; Phase 4 will
// replace with a hardhat + ethers v6 suite covering mintSchema / mintRight /
// transfer / revoke / unbound flows.

describe.skip('SignataRight (legacy suite — see Phase 4)', function () {});
